# user/artefacts/character/

Place curated standalone character artifacts here.

Format: `user/artefacts/character/<run_id>/artifact.json`  
Keep character images in the same folder and reference by `image_file`.
