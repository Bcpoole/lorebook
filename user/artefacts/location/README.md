# user/artefacts/location/

Place curated standalone location artifacts here.

Format: `user/artefacts/location/<run_id>/artifact.json`  
Keep referenced location images in the same `<run_id>/` folder.
