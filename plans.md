# GUI

## Experimentation

experiment collaspible left menu
- show different prompts and responses, allow tweaking and saving prompts, A/B testing, etc. Examples:
    - temperature and top_p variations
    - prompt template variations (some type of substition mechanism to swap out different prompt versions and test them against each other)
    - allow to set seed, but default to `9111993` for reproducibility

## Pretty + Advanced

- display an avatar for each agent (load from a local png file, each agent gets their own subdirectory, have different images based on state, current activity, mood, and silly things like temperature etc. with it defaulting to the base avatar when not specified. The smaller tweaks should have a parent structure instead of just defaulting to the base)
- Character, setting, and object generation allow to generate image via local Stable Diffusion Automatik 1111 API endpoint and display it in the UI. Prompt should be auto generated by the LLM into a Stable Diffusion Automatik 1111 compatible prompt, but allow for user tweaking and re-generation. Store the prompt and image together as part of the asset data structure.
